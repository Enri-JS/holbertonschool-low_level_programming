#include "main.h"
#include <stdio.h>
/**
 * main - prints name
 * @argv: the array of arguments
 * @argc: the number of arguments
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", *argv);

		return (0);
}
