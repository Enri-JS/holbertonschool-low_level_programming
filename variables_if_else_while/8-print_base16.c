#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char l;

	int b;

	l = 'a';
	b = 0;
	while
		(b < 10) {
			putchar(b + '0');
			b++;
		}
	while
		(l <= 'f') {
			putchar(l);
			l++;
		}
	putchar('\n');
	return (0);
}
